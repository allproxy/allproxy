'use strict';

const callScript = require('./CallScript');
module.exports = {
    callDateScript: callScript.callDateScript,
    callLevelScript: callScript.callLevelScript,
    callMessageScript: callScript.callMessageScript
}